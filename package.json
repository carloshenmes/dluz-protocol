{
  "name": "dluz-protocol",
  "version": "1.0.0",
  "description": "The Decentralized Exchange for Environmental Assets",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network baseSepolia",
    "deploy:mainnet": "hardhat run scripts/deploy.js --network base",
    "deploy:phase2:sepolia": "hardhat run scripts/deploy-phase2.js --network baseSepolia"
  },
  "keywords": [
    "defi",
    "dex",
    "carbon-credits",
    "base",
    "ethereum"
  ],
  "author": "carloshenmes",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.28.6"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.4.0",
    "dotenv": "^17.3.1"
  }
}
